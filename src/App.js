import React from 'react';
import ExampleComponent from './ExampleComponent';

/**
 * The main App component.
 * Renders all example components wrapped in MUI's ThemeProvider via index.js.
 */
function App() {
  return <ExampleComponent />;
}

export default App;
